//function declaration - 
function bomDia(value) {
    console.log(`olá, ${value}`)
}

//function expression - vc coloca, trata a funcao dentro de uma variavel, e via de regra sera uma funcoa anonima, sem nome
const boaTarde = function () {
    console.log("olá")
}

boaTarde()